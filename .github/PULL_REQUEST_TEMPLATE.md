## Summary

<!-- What does this PR do, and why? -->

## Scope

- [ ] V5 endpoint coverage:
- [ ] Version target (`v0.x.y`):
- [ ] Related tracking issue / internal ticket:

## Verification

- [ ] `npm run build` passes locally
- [ ] `npm run lint` passes locally
- [ ] `npm test` passes locally
- [ ] Manual smoke test against Bybit testnet where applicable (attach output / evidence)

## Safety checklist

- [ ] No secrets (API keys, tokens, `.env`) are committed
- [ ] No breaking changes to the public API surface — or changes are documented in `CHANGELOG.md`
- [ ] New endpoints follow existing service / naming conventions
